# Copilot Instructions

## General Guidelines
- Always use braces `{ ... }` for `if` statements, even for single-line bodies. Avoid the `if (x) y;` style going forward.

## Code Style
- Use specific formatting rules
- Follow naming conventions